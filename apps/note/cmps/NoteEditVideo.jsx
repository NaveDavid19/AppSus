export function NoteEditVideo({ selectedNote, setSelectedNote, saveNote }) {
  return (
    <section className="note-edit-wrapper">
      <section className="note-edit">
        <div>TestText</div>
      </section>
    </section>
  )
}
